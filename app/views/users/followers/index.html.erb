<% @title = "#{@user.name}のフォロワー" %>

<div class = "vertical serif tanka-index text-nowrap mt-4">
  <ul class="nav nav-tabs vertical-tab">
    <li class="nav-item">
      <%= link_to 'フォロー', users_user_followees_path(@user), class: 'nav-link vertical-tab text-secondary' %>
    </li>
    <li class="nav-item">
      <a class="nav-link active vertical-tab">フォロワー</a>
    </li>
    <li class="nav-item">
      <%= link_to '戻る', user_path(@user), class: 'nav-link vertical-tab text-secondary' %>
    </li>
  </ul>
  <%= paginate @users %>
  <%= render 'users/users', users: @users %>
  <%= paginate @users %>
</div>
