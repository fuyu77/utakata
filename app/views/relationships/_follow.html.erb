<%= turbo_frame_tag "follow_#{dom_id(user)}" do %>
  <%= link_to 'フォロー', relationships_path(user_id: user.id), data: { turbo_method: :post }, class: 'btn btn-sm btn-primary horizontal align-middle' %>&nbsp;
<% end %>
