<%= turbo_stream.replace "like_#{dom_id(@post)}" do %>
  <%= render 'favorites/like', post: @post, size: params[:size] %>
<% end %>
<%= turbo_stream.update "favorites_count_#{dom_id(@post)}" do %>
  <%= render 'favorites/favorites_count', post: @post, size: params[:size] %>
<% end %>
