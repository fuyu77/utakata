<%= turbo_frame_tag dom_id(post) do %>
  <%= link_to content_tag(:i, '', class: "lg bi bi-suit-heart-fill"), favorite_path(id: post.id, size: 'lg'), data: { turbo_method: :delete }  %>
  <%= link_to @post.followings.length, followers_post_path(id: @post.id), class: "lg like", data: { turbo: false } %>
<% end %>
