$("<%= '#like' + @post.id.to_s %>").html('<%= escape_javascript(render("favorites/favorites", post: @post, size: @size)) %>')
$("<%= '#heart' + @post.id.to_s %>").removeClass('fas')
$("<%= '#heart' + @post.id.to_s %>").addClass('far')
